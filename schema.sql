CREATE TABLE TRACKING_DATA (
            id INTEGER PRIMARY KEY,
            date DATETIME,
            name TEXT,
            distance REAL,
            steps INTEGER,
            calories REAL
        );